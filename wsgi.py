"""
Точка входа для продакшен WSGI-серверов.

Локально приложение по-прежнему запускается как раньше: `python app.py`
(см. блок `if __name__ == '__main__':` в конце app.py) — это удобно для
разработки, но сервер Flask/werkzeug там предназначен только для
локальной разработки, не для реальной нагрузки на хостинге.

На сервере вместо `python app.py` нужно использовать полноценный
WSGI-сервер, который просто импортирует объект `app` и обслуживает его:

    gunicorn -w 2 -b 0.0.0.0:$PORT wsgi:app        # Linux/macOS-хостинги
    waitress-serve --port=$PORT wsgi:app            # если хостинг на Windows

Именно поэтому здесь нет ничего, кроме импорта — вся конфигурация уже
сделана в app.py (пути к БД, secret_key, JSON_SORT_KEYS и т.д.).
"""

from app import app

if __name__ == '__main__':
    app.run()
